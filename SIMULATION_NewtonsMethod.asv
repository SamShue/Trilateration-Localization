clc;
clear all;
close all;

% plot function to be solved
v = -1:0.1:1;
plot(v', f(v));

x_init = 2;
f(x);

for




function [y] = f(x)
    y = x.^7 - 1000;
end

function [y] = f_prime(x)
    y = 7.*x.^6;
end